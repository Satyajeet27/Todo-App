<!DOCTYPE html>
<html lang="en">
  <%- include("./partials/head") %>
  <body class="bg-dark-subtle">
    <%- include("./partials/navbar") %>
    <div class="container">
      <div
        class="card shadow w-50 mx-auto mt-5 px-4 bg-body-secondary"
        id="custom-card"
      >
        <p class="fs-4 fw-bold text-center text-primary-emphasis mt-2">
          Update your work status
        </p>
        <div class="d-flex flex-column">
          <h6 class="text-secondary-emphasis">
            Work:
            <span class="fs-5 text-dark"><%=locals.items.workItem %></span>
          </h6>
          <h6 class="text-secondary-emphasis">
            Created At:
            <span class="fw-light text-dark"
              ><%=locals.items.createdAt %>
            </span>
          </h6>
        </div>

        <form action="/todo/update" method="post">
          <div class="d-flex">
            <h6 class="pe-2 text-secondary-emphasis">Completed</h6>
            <input
              type="checkbox"
              name="checkbox"
              class="form-check-input border border-secondary"
              value="<%=locals.items._id %>"
            />
          </div>
          <div class="text-center">
            <button
              type="submit"
              class="btn bg-success bg-gradient text-light w-25 mb-3 fw-bold"
            >
              Save
            </button>
          </div>
        </form>
      </div>
    </div>
    <script
      src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha2/dist/js/bootstrap.bundle.min.js"
      integrity="sha384-qKXV1j0HvMUeCBQ+QVp7JcfGl760yU08IQ+GpUo5hlbpg51QRiuqHAJz8+BrxE/N"
      crossorigin="anonymous"
    ></script>
  </body>
</html>
